<% title "タイムライン" %>

<div class="container">
  <div class="row">
		<div class="row">
		  <% if !@posts.empty? %>
		    <% @posts.each do |post| %>
          <% if post.user.unsubscribe == false %>
				    <div class="col-xs-4 p-0" style="text-align: center;">
        			<%= link_to post_path(post.id) do %>
						    <%= attachment_image_tag post, :image, format: 'jpeg', fallback:"noimage2.jpeg", size:"250x250" %>
              <% end %>
        		</div>
          <% end %>
        <% end %>
      <% else %>
        <h3 style="text-align: center; padding-top: 100px;">
          まだフォローしているユーザーがいません。<br>
          「投稿検索」してお気に入りユーザーをフォローしましょう！
        </h3>
      <% end %>
  	</div>
  </div>
</div>